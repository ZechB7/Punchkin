const {Player} = require('../../../models/player');
const sequelize = require('../../../config/connection');

class Hands extends Player {
    // can not be greater than two, reads play field
}

module.express = Hands;