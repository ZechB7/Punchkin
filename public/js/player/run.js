const {Player} = require('../../../models/player');
const sequelize = require('../../../config/connection');

class Run extends Player {
    // if player run exists add to math.floor(6)
    // execute run if player beats monster run

}

module.exports = Run;