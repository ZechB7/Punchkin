const {Player} = require('../../../models/player');
const sequelize = require('../../../config/connection');

class Strength extends Player {
    // if player level + bonus > monster level + bonus
    // defeat monster recieve reward

    // else take bad stuff

}